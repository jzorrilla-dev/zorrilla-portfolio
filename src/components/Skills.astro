---
import ReactIcon from './icons/ReactIcon.astro'
import AstroIcon from './icons/AstroIcon.astro'
import TailwindIcon from './icons/TailwindIcon.astro'
import LaravelIcon from './icons/LaravelIcon.astro'
import NodeIcon from './icons/NodeJSIcon.astro'
import PostgresIcon from './icons/PostgresIcon.astro'
import DockerIcon from './icons/DockerIcon.astro'
import JavaScriptIcon from './icons/JavaScriptIcon.astro'
import TypeScriptIcon from './icons/TypeScriptIcon.astro'
import PHPIcon from './icons/PHPIcon.astro'
import NextJSIcon from './icons/NextJSIcon.astro'
import AlpineJSIcon from './icons/AlpineJSIcon.astro'

type IconKey = keyof typeof iconMap

const iconMap = {
  react: ReactIcon,
  astro: AstroIcon,
  tailwind: TailwindIcon,
  laravel: LaravelIcon,
  alpine: AlpineJSIcon,
  node: NodeIcon,
  postgresql: PostgresIcon,
  docker: DockerIcon,
  javascript: JavaScriptIcon,
  typescript: TypeScriptIcon,
  php: PHPIcon,
  next: NextJSIcon
}

const allSkills: { name: string; icon: IconKey }[] = [
  { name: 'React', icon: 'react' },
  { name: 'Astro', icon: 'astro' },
  { name: 'Tailwind CSS', icon: 'tailwind' },
  { name: 'Laravel', icon: 'laravel' },
  { name: 'Alpine.js', icon: 'alpine' },
  { name: 'Node.js', icon: 'node' },
  { name: 'PostgreSQL', icon: 'postgresql' },
  { name: 'Docker', icon: 'docker' },
  { name: 'JavaScript', icon: 'javascript' },
  { name: 'TypeScript', icon: 'typescript' },
  { name: 'PHP', icon: 'php' },
  { name: 'Next.js', icon: 'next' }
]
---

<section
  id="habilidades"
  class="py-20 sm:py-32 bg-gray-50 dark:bg-gray-900 transition-colors duration-500"
>
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <h2 class="text-4xl font-extrabold text-gray-900 dark:text-white mb-16 text-center">
      Mi Stack Tecnológico
    </h2>

    <div class="overflow-hidden">
      <div class="flex gap-4 sm:gap-6 whitespace-nowrap animate-skill-scroll-left hover:paused">
        {
          allSkills.map(({ name, icon }) => {
            const IconComponent = iconMap[icon]
            if (!IconComponent) return null

            return (
              <div class="flex-shrink-0 min-w-[140px] sm:min-w-[160px] md:min-w-[180px] p-2">
                <div class="flex flex-col items-center p-4 sm:p-6 bg-gray-100 dark:bg-gray-800 rounded-xl shadow-lg hover:shadow-cyan-500/50 transition-all duration-300 transform hover:-translate-y-2 border border-gray-200 dark:border-gray-700">
                  <div class="w-14 h-14 sm:w-16 sm:h-16 mb-3 sm:mb-4 flex items-center justify-center">
                    <IconComponent />
                  </div>
                  <p class="text-sm sm:text-base md:text-lg font-semibold text-gray-800 dark:text-gray-200 text-center">
                    {name}
                  </p>
                </div>
              </div>
            )
          })
        }

        {
          allSkills.map(({ name, icon }) => {
            const IconComponent = iconMap[icon]
            if (!IconComponent) return null

            return (
              <div class="flex-shrink-0 min-w-[140px] sm:min-w-[160px] md:min-w-[180px] p-2">
                <div class="flex flex-col items-center p-4 sm:p-6 bg-gray-100 dark:bg-gray-800 rounded-xl shadow-lg hover:shadow-cyan-500/50 transition-all duration-300 transform hover:-translate-y-2 border border-gray-200 dark:border-gray-700">
                  <div class="w-14 h-14 sm:w-16 sm:h-16 mb-3 sm:mb-4 flex items-center justify-center">
                    <IconComponent />
                  </div>
                  <p class="text-sm sm:text-base md:text-lg font-semibold text-gray-800 dark:text-gray-200 text-center">
                    {name}
                  </p>
                </div>
              </div>
            )
          })
        }
      </div>
    </div>
  </div>
</section>
